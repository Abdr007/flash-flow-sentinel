{
  "name": "flash-flow-sentinel",
  "version": "1.0.0",
  "private": true,
  "description": "Flash V2 (FLASH6) real-time vault inflow/outflow monitor with per-hour outflow limits — 100% on-chain data, no synthetic values.",
  "main": "sentinel.js",
  "scripts": {
    "start": "node sentinel.js",
    "verify": "node verify.cjs",
    "probe": "node probe.cjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.30.1"
  },
  "overrides": {
    "uuid": "8.3.2"
  }
}
